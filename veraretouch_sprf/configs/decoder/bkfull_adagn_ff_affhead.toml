[paths]
# veraretouch_sprf 主线配置（EPR-052）：由 experiments/prs/EPR-051_masked-restore-production/stage0/sprf/configs/bkfull_adagn_ff_affhead.toml 机械派生；去掉本 [paths] 节后与原件逐字节相同（scripts/check_configs.py 核对）。
# 路径占位 ${paths.<key>} 由 veraretouch_sprf.configs.materialize() 解析；环境变量 VR_PATH_<KEY> 可覆盖。
repo = "/home/bc/VeraRetouch"
runs_sprf = "/home/bc/data/runs/epr051_sprf"
runs = "/home/bc/data/runs"
shards_local = "/home/bc/data/builds/epr051_stage0"
shards_nfs = "/mnt/nfs-ro/bc/data/datasets/epr051_stage0"
lut_bank = "/var/cache/veradata/preset_bank_full"
models = "/home/bc/data/models"
# --- end [paths] ---
# EPR-051 · BK-FULL：ADAGN+FF+AFFHEAD 按 C-LUT-FULL 全量配方（由 arm_clut_full.toml 机械派生，只加 [bk] 节与 arm/run 身份键）。
# EPR-051 · stage0 · arm=sprf —— C-LUT：逆 LUT 网格 latent（oracle_lut；其余同 Chain-end）
# 由 configs/make_configs.py 生成，请勿手改：同组内除 flow.path_mode /
# flow.endpoint_probability / flow.alpha_mode / solver.nfe_per_stage 与三个
# run 身份键外必须逐键相同（train_sprf 启动断言 A3）。
# 实验语义数字全部在本文件；train_sprf.py 里没有任何默认值兜底（缺键 = 停机）。

[arm]
name          = "sprf_bkfull_adagn_ff_affhead"
# 本臂无同组对照臂（C-REF 已取消、C-LUT 是 v2 数据），A3 组内比对因此为空集。
# 与 C-LUT 的跨组差异由 NOTES D-xc14 逐键钉死，不靠 A3。
peer_configs  = []
# 允许臂间不同的非实验语义键（run 身份），其余任何差异都是 A3 的 blocker。
identity_keys = ["arm.name", "arm.peer_configs", "run.out_dir"]

[run]
name              = "epr051_sprf"
out_dir           = "${paths.runs_sprf}/bkfull_adagn_ff_affhead"
device            = "cuda:0"          # payload 恒 cuda:0（CUDA_VISIBLE_DEVICES 重映射后）
seed              = 20260830
max_steps         = 20000
one_pass_cap      = false   # 20000x64=1.28M draws ≈ 1.95 epoch over 655,461，禁静默截断
log_every         = "auto"
eval_every        = 400
ckpt_every        = "auto"
auto_log_divisor  = 30
auto_eval_divisor = 10
eval_at           = []

[data]
shards             = [
  "${paths.shards_nfs}/shard-0023",
  "${paths.shards_nfs}/shard-0024",
  "${paths.shards_nfs}/shard-0025",
  "${paths.shards_nfs}/shard-0026",
  "${paths.shards_nfs}/shard-0027",
  "${paths.shards_local}/shard-0028",
  "${paths.shards_local}/shard-0029",
  "${paths.shards_local}/shard-0030",
  "${paths.shards_local}/shard-0031",
  "${paths.shards_local}/shard-0032",
  "${paths.shards_local}/shard-0033",
  "${paths.shards_local}/shard-0034",
  "${paths.shards_local}/shard-0035",
  "${paths.shards_local}/shard-0036",
  "${paths.shards_local}/shard-0037",
  "${paths.shards_local}/shard-0038",
  "${paths.shards_local}/shard-0039",
  "${paths.shards_local}/shard-0040",
  "${paths.shards_local}/shard-0041",
  "${paths.shards_local}/shard-0042",
  "${paths.shards_local}/shard-0043",
  "${paths.shards_local}/shard-0044",
  "${paths.shards_local}/shard-0045",
  "${paths.shards_local}/shard-0046",
  "${paths.shards_local}/shard-0047",
  "${paths.shards_local}/shard-0048",
  "${paths.shards_local}/shard-0049",
  "${paths.shards_local}/shard-0050",
  "${paths.shards_local}/shard-0051",
  "${paths.shards_local}/shard-0052",
  "${paths.shards_local}/shard-0053",
  "${paths.shards_local}/shard-0054",
  "${paths.shards_local}/shard-0055",
  "${paths.shards_local}/shard-0056",
  "${paths.shards_local}/shard-0057",
  "${paths.shards_nfs}/shard-0064",
  "${paths.shards_nfs}/shard-0065",
  "${paths.shards_nfs}/shard-0066",
  "${paths.shards_nfs}/shard-0067",
  "${paths.shards_nfs}/shard-0068",
  "${paths.shards_nfs}/shard-0069",
  "${paths.shards_nfs}/shard-0070",
  "${paths.shards_nfs}/shard-0071",
  "${paths.shards_nfs}/shard-0072",
  "${paths.shards_nfs}/shard-0073",
  "${paths.shards_nfs}/shard-0074",
  "${paths.shards_nfs}/shard-0075",
  "${paths.shards_nfs}/shard-0076",
  "${paths.shards_nfs}/shard-0077",
  "${paths.shards_nfs}/shard-0078",
  "${paths.shards_nfs}/shard-0079",
  "${paths.shards_nfs}/shard-0080",
  "${paths.shards_nfs}/shard-0081",
  "${paths.shards_nfs}/shard-0082",
  "${paths.shards_nfs}/shard-0083",
  "${paths.shards_nfs}/shard-0084",
  "${paths.shards_nfs}/shard-0085",
  "${paths.shards_nfs}/shard-0086",
  "${paths.shards_nfs}/shard-0087",
  "${paths.shards_nfs}/shard-0088",
  "${paths.shards_nfs}/shard-0089",
  "${paths.shards_nfs}/shard-0090",
  "${paths.shards_nfs}/shard-0091",
  "${paths.shards_nfs}/shard-0092",
  "${paths.shards_nfs}/shard-0093",
  "${paths.shards_nfs}/shard-0094",
  "${paths.shards_nfs}/shard-0095",
  "${paths.shards_nfs}/shard-0096",
  "${paths.shards_nfs}/shard-0097",
  "${paths.shards_nfs}/shard-0098",
  "${paths.shards_nfs}/shard-0099",
  "${paths.shards_nfs}/shard-0100",
  "${paths.shards_nfs}/shard-0101",
  "${paths.shards_nfs}/shard-0102",
  "${paths.shards_nfs}/shard-0103",
  "${paths.shards_nfs}/shard-0104",
  "${paths.shards_nfs}/shard-0105",
  "${paths.shards_nfs}/shard-0106",
  "${paths.shards_nfs}/shard-0107",
  "${paths.shards_nfs}/shard-0108",
  "${paths.shards_nfs}/shard-0109",
  "${paths.shards_nfs}/shard-0110",
  "${paths.shards_nfs}/shard-0111",
  "${paths.shards_nfs}/shard-0112",
  "${paths.shards_nfs}/shard-0113",
  "${paths.shards_nfs}/shard-0114",
  "${paths.shards_nfs}/shard-0115",
  "${paths.shards_nfs}/shard-0116",
  "${paths.shards_nfs}/shard-0117",
  "${paths.shards_nfs}/shard-0118",
  "${paths.shards_nfs}/shard-0119",
  "${paths.shards_nfs}/shard-0120",
  "${paths.shards_nfs}/shard-0121",
  "${paths.shards_nfs}/shard-0122",
  "${paths.shards_nfs}/shard-0123",
  "${paths.shards_nfs}/shard-0124",
  "${paths.shards_nfs}/shard-0125",
  "${paths.shards_nfs}/shard-0126",
  "${paths.shards_nfs}/shard-0127",
  "${paths.shards_nfs}/shard-0128",
  "${paths.shards_nfs}/shard-0129",
  "${paths.shards_nfs}/shard-0130",
  "${paths.shards_nfs}/shard-0131",
  "${paths.shards_nfs}/shard-0132",
  "${paths.shards_nfs}/shard-0133",
  "${paths.shards_nfs}/shard-0134",
  "${paths.shards_nfs}/shard-0135",
  "${paths.shards_nfs}/shard-0136",
  "${paths.shards_nfs}/shard-0137",
  "${paths.shards_nfs}/shard-0138",
  "${paths.shards_nfs}/shard-0139",
  "${paths.shards_nfs}/shard-0140",
  "${paths.shards_nfs}/shard-0141",
  "${paths.shards_nfs}/shard-0142",
  "${paths.shards_nfs}/shard-0143",
  "${paths.shards_nfs}/shard-0144",
  "${paths.shards_nfs}/shard-0145",
  "${paths.shards_nfs}/shard-0146",
  "${paths.shards_nfs}/shard-0147",
  "${paths.shards_nfs}/shard-0148",
  "${paths.shards_nfs}/shard-0149",
  "${paths.shards_nfs}/shard-0150",
  "${paths.shards_nfs}/shard-0151",
  "${paths.shards_nfs}/shard-0152",
  "${paths.shards_nfs}/shard-0153",
  "${paths.shards_nfs}/shard-0154",
  "${paths.shards_nfs}/shard-0155",
  "${paths.shards_nfs}/shard-0156",
  "${paths.shards_nfs}/shard-0157",
  "${paths.shards_nfs}/shard-0158",
  "${paths.shards_nfs}/shard-0159",
  "${paths.shards_nfs}/shard-0160",
  "${paths.shards_nfs}/shard-0161",
  "${paths.shards_nfs}/shard-0162",
  "${paths.shards_nfs}/shard-0163",
  "${paths.shards_nfs}/shard-0164",
  "${paths.shards_nfs}/shard-0165",
  "${paths.shards_nfs}/shard-0166",
  "${paths.shards_nfs}/shard-0167",
  "${paths.shards_nfs}/shard-0168",
  "${paths.shards_nfs}/shard-0169",
  "${paths.shards_nfs}/shard-0170",
  "${paths.shards_nfs}/shard-0171",
  "${paths.shards_nfs}/shard-0172",
  "${paths.shards_nfs}/shard-0173",
  "${paths.shards_nfs}/shard-0174",
  "${paths.shards_nfs}/shard-0175",
  "${paths.shards_nfs}/shard-0176",
  "${paths.shards_nfs}/shard-0177",
  "${paths.shards_nfs}/shard-0178",
  "${paths.shards_nfs}/shard-0179",
  "${paths.shards_nfs}/shard-0180",
  "${paths.shards_nfs}/shard-0181",
  "${paths.shards_nfs}/shard-0182",
  "${paths.shards_nfs}/shard-0183",
  "${paths.shards_nfs}/shard-0184",
  "${paths.shards_nfs}/shard-0185",
  "${paths.shards_nfs}/shard-0186",
  "${paths.shards_nfs}/shard-0187",
  "${paths.shards_nfs}/shard-0188",
  "${paths.shards_nfs}/shard-0189",
  "${paths.shards_nfs}/shard-0190",
  "${paths.shards_nfs}/shard-0191",
  "${paths.shards_nfs}/shard-0192",
  "${paths.shards_nfs}/shard-0193",
  "${paths.shards_nfs}/shard-0194",
  "${paths.shards_nfs}/shard-0195",
  "${paths.shards_nfs}/shard-0196",
  "${paths.shards_nfs}/shard-0197",
  "${paths.shards_nfs}/shard-0198",
  "${paths.shards_nfs}/shard-0199",
  "${paths.shards_nfs}/shard-0200",
  "${paths.shards_nfs}/shard-0201",
  "${paths.shards_nfs}/shard-0202",
  "${paths.shards_nfs}/shard-0203",
  "${paths.shards_nfs}/shard-0204",
  "${paths.shards_nfs}/shard-0205",
  "${paths.shards_nfs}/shard-0206",
  "${paths.shards_nfs}/shard-0207",
  "${paths.shards_nfs}/shard-0208",
  "${paths.shards_nfs}/shard-0209",
  "${paths.shards_nfs}/shard-0210",
  "${paths.shards_nfs}/shard-0211",
  "${paths.shards_nfs}/shard-0212",
  "${paths.shards_nfs}/shard-0213",
  "${paths.shards_nfs}/shard-0214",
  "${paths.shards_nfs}/shard-0215",
  "${paths.shards_nfs}/shard-0216",
  "${paths.shards_nfs}/shard-0217",
  "${paths.shards_nfs}/shard-0218",
  "${paths.shards_nfs}/shard-0219",
  "${paths.shards_nfs}/shard-0220",
  "${paths.shards_nfs}/shard-0221",
  "${paths.shards_nfs}/shard-0222",
  "${paths.shards_nfs}/shard-0223",
  "${paths.shards_nfs}/shard-0224",
  "${paths.shards_nfs}/shard-0225",
  "${paths.shards_nfs}/shard-0226",
  "${paths.shards_nfs}/shard-0227",
  "${paths.shards_nfs}/shard-0228",
  "${paths.shards_nfs}/shard-0229",
  "${paths.shards_nfs}/shard-0230",
  "${paths.shards_nfs}/shard-0232",
  "${paths.shards_nfs}/shard-0233",
  "${paths.shards_nfs}/shard-0235",
  "${paths.shards_nfs}/shard-0236",
  "${paths.shards_nfs}/shard-0237",
  "${paths.shards_nfs}/shard-0239",
  "${paths.shards_nfs}/shard-0240",
  "${paths.shards_nfs}/shard-0241",
  "${paths.shards_nfs}/shard-0243",
  "${paths.shards_nfs}/shard-0244",
  "${paths.shards_nfs}/shard-0245",
  "${paths.shards_nfs}/shard-0247",
  "${paths.shards_nfs}/shard-0248",
  "${paths.shards_nfs}/shard-0249",
  "${paths.shards_nfs}/shard-0251",
  "${paths.shards_nfs}/shard-0252",
  "${paths.shards_nfs}/shard-0253",
  "${paths.shards_nfs}/shard-0255",
  "${paths.shards_nfs}/shard-0256",
  "${paths.shards_nfs}/shard-0257",
  "${paths.shards_nfs}/shard-0259",
  "${paths.shards_nfs}/shard-0260",
  "${paths.shards_nfs}/shard-0262",
  "${paths.shards_nfs}/shard-0264",
  "${paths.shards_nfs}/shard-0265",
  "${paths.shards_nfs}/shard-0266",
  "${paths.shards_nfs}/shard-0268",
  "${paths.shards_nfs}/shard-0269",
  "${paths.shards_nfs}/shard-0270",
  "${paths.shards_nfs}/shard-0272",
  "${paths.shards_nfs}/shard-0273",
  "${paths.shards_nfs}/shard-0274",
  "${paths.shards_nfs}/shard-0276",
  "${paths.shards_nfs}/shard-0277",
  "${paths.shards_nfs}/shard-0278",
  "${paths.shards_nfs}/shard-0280",
  "${paths.shards_nfs}/shard-0281",
  "${paths.shards_nfs}/shard-0282",
  "${paths.shards_nfs}/shard-0284",
  "${paths.shards_nfs}/shard-0285",
  "${paths.shards_nfs}/shard-0286",
  "${paths.shards_nfs}/shard-0288",
  "${paths.shards_nfs}/shard-0289",
  "${paths.shards_nfs}/shard-0290",
  "${paths.shards_nfs}/shard-0292",
  "${paths.shards_nfs}/shard-0293",
  "${paths.shards_nfs}/shard-0295",
  "${paths.shards_nfs}/shard-0297",
  "${paths.shards_nfs}/shard-0298",
  "${paths.shards_nfs}/shard-0299",
  "${paths.shards_nfs}/shard-0301",
  "${paths.shards_nfs}/shard-0302",
  "${paths.shards_nfs}/shard-0303",
  "${paths.shards_nfs}/shard-0305",
  "${paths.shards_nfs}/shard-0306",
  "${paths.shards_nfs}/shard-0307",
  "${paths.shards_nfs}/shard-0309",
  "${paths.shards_nfs}/shard-0310",
  "${paths.shards_nfs}/shard-0311",
  "${paths.shards_nfs}/shard-0313",
  "${paths.shards_nfs}/shard-0314",
  "${paths.shards_nfs}/shard-0315",
  "${paths.shards_nfs}/shard-0317",
  "${paths.shards_nfs}/shard-0318",
  "${paths.shards_nfs}/shard-0319",
  "${paths.shards_nfs}/shard-0321",
  "${paths.shards_nfs}/shard-0322",
  "${paths.shards_nfs}/shard-0325",
  "${paths.shards_nfs}/shard-0326",
  "${paths.shards_nfs}/shard-0330",
  "${paths.shards_nfs}/shard-0331",
  "${paths.shards_nfs}/shard-0334",
  "${paths.shards_nfs}/shard-0335",
  "${paths.shards_nfs}/shard-0338",
  "${paths.shards_nfs}/shard-0339",
  "${paths.shards_nfs}/shard-0342",
  "${paths.shards_nfs}/shard-0343",
]
asset_source       = "dir"
clean_only         = true
depth_flags_ok     = ["clean"]
clean_index_names  = ["clean_index.by_depth.json", "clean_index.json"]
split_salt         = "epr051-stage0"
holdout_bucket_min = 95
require_split_rules = ["frozen_table", "sha1_verasplit-v1"]
split_table         = "${paths.repo}/tools/data_splits/splits_sources.csv"
train_bucket_max    = 89
num_workers         = 12
# 目标构造要 LUT 身份：bank 与每进程的 LUT 体 LRU 上限。
lut_bank_dir        = "${paths.lut_bank}"
lut_cache_size      = 64
# 本数据律下每条链同时落 d4/d5/d6（snapshot v2 的 emit_spec）。
depth_values        = [4, 5, 6]

[guard]
build_tool_sha256_allowed = [
  "d7171588958533227a81ca127dd513a461ca6fece469cecd4e664b21c03fb1ee",
]
build_config_sha256_allowed = [
  "d857b685855decde3e29625623edec9e144e03b360c8660b5bad08ad5b86d202",
]
# 渲染律本体（mix_alpha）——前向机械 import 原件，sha 冻在第一步前。
lutdata_sha256 = "50fe6cf5f18ab547bdf65998af4f4a52530f1d8cd0313bcf5fa3ab5c46ec9169"

[encoder]
# C0：冻结 pooled read-out，首轮一律不动 conditioner（C1–C3 只注册不实现）。
path        = "${paths.models}/siglip2-so400m-patch14-384"
readout     = "pooler"
inputs      = ["after"]               # headline：模型只看退化图
dtype       = "bf16"
cache_batch = 32
cache_dir   = "${paths.runs_sprf}/feature_cache"

[flow]
# —— 以下四个是臂间唯一允许不同的预注册键 ——
path_mode             = "chain"
endpoint_probability  = 1.0
alpha_mode            = "alpha_full"
# —— 以上 ——
width            = 512
n_layers         = 8
n_layers_allowed = [4, 5, 6, 8]   # 提案容量节 v2：主臂 8 层，4–6 是容量下界消融
cond_hidden      = 256
cond_layers      = 4
# K5 的 LUT-ID 条件诊断臂。off = 首版（推理端无 LUT 身份）；
# embed 只用于 oracle 诊断行，此时 restore() 拒绝当生产接口用。
# D-quant 裁决的锚点契约（预注册键，train_sprf 启动断言 A10）：
# rollout loss 与推理起点 = uint8 资产 y（与部署一致）；
# teacher-forced 采样 = fp32 链态 r^m。两者差一个已知量化位（≈0.5/255），不混用。
rollout_anchor   = "asset_y"
teacher_anchor   = "chain_r"
anchor_quant_gap_max = 0.00393   # = 1/255，允许 <=1 个 8bit 级；超了即口径漂移
# T5 B-g4d：action 表示消融（mlp = 逐像素 1x1 conv；g4d = EPR-028 载体）。
action_backend   = "mlp"
lut_id_condition = "off"
lut_id_vocab     = 4096

[g4d]
# 只有 flow.action_backend = "g4d" 时才实例化；mlp 臂读了也不建模块。
mode        = "A3"
n_gauss     = 48                  # 27*48 + 12 = 1308 个 θ
hidden      = 512
init_seed   = 20260810            # 抄 G4DConfig 的默认，恒等 init 可复现
point_chunk = 2048

[ft]
# T6b：从 VeraRetouch 预训练 ckpt 暖启动；encoder 在线前向（不用 feature cache）。
init_from_ckpt  = "${paths.runs}/epr051_new_confpool"
cache           = "off"
finetune_scope  = "lora_readout_trunk_render"
inject_hidden   = 256
# E4 裁决：预训练载体指向 clean，action 必须是 C(z)-z；s 取 α̂（generator 原生输入）。
action_sign     = "carrier_minus_z"
carrier_s       = "alpha_hat"
# E5 裁决：auto = PEFT "all-linear"（只自动选注入位置）；r/alpha 锚 EPR-033 钉死值。
lora_target_modules = "all-linear"
lora_r          = 8
lora_alpha      = 16
lora_dropout    = 0.0

[solver]
nfe_per_stage = 1       # 预注册键（1 = Euler，2 = 中点，NFE sweep）
clamp_lo      = 0.0
clamp_hi      = 1.0

[loss]
pixels_per_sample = 8192
w_prev   = 1.0
w_action = 0.25
w_clean  = 0.1
w_bound  = 0.01
w_rollout = 0.5
w_q50     = 0.05
# B3 损失形式（预注册）：prev/clean/rollout = Charbonnier，action = SmoothL1，
# bound = ReLU(|ĥ|-1) 的平方。两个形状参数是 TOML 键，代码里无默认值。
charbonnier_eps = 1e-6
smooth_l1_beta  = 0.05            # ĥ∈[-1,1]，β=1 会退化成纯 L2；见 NOTES D-sl1
action_abs_bound        = 1.0     # h* ∈ [-1,1]^3（构造代数事实）
# N1：rollout 从 30% budget 起，batch 比例走**两段阶跃** 25% -> 50%
rollout_start_frac      = 0.3
rollout_step_frac       = 0.65    # 阶跃点：此前 25%，此后 50%
rollout_batch_frac_start = 0.25
rollout_batch_frac_end   = 0.5
q50_start_frac = 0.7              # 最后 30%
q50_band_lo    = 0.45             # Q45–Q55 分位带
q50_band_hi    = 0.55

[optim]
lr             = 3e-4
weight_decay   = 0.01
betas          = [0.9, 0.999]
batch          = 64
schedule       = "cosine"
warmup_steps   = "auto"
warmup_frac    = 0.05
warmup_max     = 500
grad_clip      = 1.0
autocast       = "bf16"
# rollout 反传要留 d 阶段 × n_layers 层的激活；batch64×8192px×w512×8层×6阶段
# 实测 OOM（68.5 GiB）。梯度检查点只存阶段边界、反传重算层内激活，
# **数学与梯度不变**，换显存。
rollout_grad_checkpoint = true
autocast_scope = "trunk_only"      # 只包 conditioner trunk；action 与更新路径恒 fp32

[eval]
pixels_per_sample    = 100000
interval_max_samples = 128
final_max_samples    = 0
subset_salt          = "epr051-stage0-eval"
# B5：Δ_shuffle 的错位对象必须跨 source_id（同源不同 depth 的条件近乎等价）。
shuffle_rule         = "cross_source_sha1_next"
shuffle_salt         = "epr051-sprf-shuffle:"
# 提案第 3 节评测列全表；MetricLedger 逐列 assert_wired（调用数 == 被评样本数）。
# true_lut_condition_delta 需 flow.lut_id_condition = "embed"；
# predicted_lut_accuracy 需挂 LUT 辅助分类头。首版都不挂 -> 两列不注册，
# 在 metrics.json 的 columns_declared_unavailable 里注明（不出假数）。
columns              = [
  "model", "identity", "exact_solve", "model_midpoint", "active_only_headline",
  "stage_all", "stage_active", "stage_beta1", "cycle",
  "rollout_drift", "mask_leak_max", "mask_bitexact_count",
  "stage_oob_fraction", "stage_oob_max", "condition_number_by_stage",
  "nfe_d_vs_2d", "latency_by_depth", "delta_const", "delta_shuffle",
  "stage_mask_bitexact",
  # X-COND：ΔE00 主列 + 恒等对照行。de00 从构造工具 import 原件
  # （epr050_build_degradation.de00 = skimage deltaE_ciede2000，与构造侧
  # 标定 s 时用的是同一个函数）。
  "de00", "de00_frac_le5", "de00_identity", "de00_identity_frac_le5",
  # 编辑侧负控制（战役硬规则：每个消融行必带负控制）。作用对象是**编辑描述子**
  # 本身，不是 SigLIP 的 c —— 后者已有 delta_const / delta_shuffle。
  #   null: 描述子置零     -> 「用没用编辑信息」
  #   roll: 沿阶段轴移一位 -> 「用没用**对**的那条编辑」（样本/容量都不变）
  "delta_edit_null", "delta_edit_roll",
]
# ΔE00 走 skimage，10 万像素 × 4560 样本不可行；按**确定性等距**子采样
# （不是随机抽）到这个上限，实际像素数逐样本落盘。
de00_max_pixels      = 4096
strata               = ["depth", "geom", "rec_band"]
# 结构性稀疏层：β = 场 × s，s≠1 的链上没有 β==1 的像素（mix_alpha 的
# alpha==1 分支不触发），该列会在整批样本上为空。只放宽「全体 missing 即停机」
# 一条，调用计数与单一来源两条守卫不变；n_available 照样落盘。
# stage_mask_bitexact 同理：本数据律的 global 步让 union 覆盖全图
# （union_frac=1.0），linear/one_shot 臂逐阶段 β_m==0 的像素集为空 —— 分母空，
# 不是没算（调用计数照常）。见 NOTES D-mask。
sparse_ok_columns    = ["stage_beta1", "stage_mask_bitexact"]
exact_solve_source   = "journal"
select_metric        = "heldout_model_linf8_p50"   # 禁 val loss / 禁 AUC

[diag]
# condition_number_by_stage：κ₂((1-β)I + βJ_L)，FD 口径抄 epr050::_gn，
# σ_min < sigma_floor 的像素显式排除（preflight B1：不做 clamp 兜底）。
condition_number_enabled = true
condition_number_max_pixels = 4096
svd_chunk    = 200000
sigma_floor  = 1e-12
fd_clamp_lo  = 0.0
fd_clamp_hi  = 1.0
fd_min_h     = 1e-8

[assert]
teacher_check_every = 100     # 每 N 个 batch 抽 1 个样本独立重算
teacher_tol         = 1e-6         # 沿 preflight G-A1 / G-A2 的 tol
teacher_rho         = [0.13, 0.5, 0.87]
# 目标构造在 DataLoader worker 的 CPU 上做；逐位比对必须同设备
# （同一样本 GPU 与 CPU 的 fp32 链差 1.19e-07~1.79e-07，preflight D3/D9）。
teacher_device      = "cpu"
# A1 的 g4d 两界：g4d 恒等 init 只保证 sum_i w_i ≈ 1，不是逐位恒等。
a1_g4d_abs_tol        = 2e-3
a1_g4d_max_level_diff = 1

[edit]
# X-COND：显式编辑信息注入。用户假设 —— 模型只见退化图、不知道「编辑是什么」，
# 信息瓶颈在编辑操作不可见。本节把编辑信息用 oracle 方式显式喂进去。
#   inv_lut   该阶段真实 LUT 的**逆表**（直接给编辑）        契约 oracle_lut
#   ref_pair  该阶段真实 LUT 的**演示对**（从演示里提编辑）  契约 oracle_ref
# 两档的描述子逐字同形 (grid³, 4)：通道 0..2 位移、通道 3 有效/占用位，
# 编码器结构与参数量因此在两臂上完全一致（J-C3 读的是来源差，不是容量差）。
# **两臂都写满两档的键**，所以组内 A3 的 diff 正好是 condition + contract。
condition      = "inv_lut"
contract       = "oracle_lut"
grid           = 17               # 17³ = 4913 格；逆表与演示对分箱共用同一个格
latent_dim     = 128              # per-stage latent，与 c 在 FiLM 头入口**拼接**
enc_hidden     = 512
enc_layers     = 3                # 2-3 层 MLP，普通 init（FiLM 头仍零初始化 -> A1 不变）
# inv_lut：precompute_inv_lut.py 的产物（按 lut_id 一次性求逆，**不逐图求逆**）。
# 指纹（bank / build 工具 / build config / INV_ITERS / INV_EARLY / 表体 sha256）
# 在启动时逐项比对，任一漂了即停机。
inv_cache_dir  = "${paths.runs_sprf}/lut_inv_cache/g17"
# ref_pair：参考池从 **train 侧**按 source_id 去重等距抽，选谁走
# sha1(salt + sample_id) 后向前找第一个跨 source 的成员（禁与本样本同源）。
ref_pool_n     = 256
ref_size       = 256
ref_salt       = "epr051-sprf-refpair:"
ref_hist_bins  = 256              # 直方图匹配的 CDF 级数（资产本来就是 8bit）

[subset]
# T-ALIGN/XL 的**新预注册**训练子集规则（用户裁决 2026-08-31 采纳 300k）。
# 与快照里的 200k epoch 规则**并存不替代**：那条是 epoch 语义（每轮走哪 200k），
# 本条是单臂训练子集语义（这条臂总共只见这 300k）。各有各的 sha。
# 「跑全量的 300k」按字面取 300k 子集；v3 全量 655,461 的选项留档未取。
enabled   = true
# W1 全量：不筛 TRAIN（train_subset=false），但 held-out **仍按冻结名单收窄** ——
# 不收窄的话 load_shards 会把新分片 held-out 桶行也算进来，评测集 4,560 -> ~35,571，
# 与 C-LUT(4.758) / XL(1.2445) 不可比（D-xc14）。
train_subset = false
n         = 655461
salt      = "epr051-v3-clutxl"
sample_key = 'f"{id}|d{depth}"  (depth "full" when the row has no emits[])'
rule      = 'sort TRAIN samples by sha1("epr051-v3-clutxl:" + sample_key) hex ascending, take the first 300000'
keys_file = "${paths.repo}/experiments/prs/EPR-051_masked-restore-production/stage0/snapshot_newdata_v3.clutxl300k_keys.json"
sha256    = "510755f95c24703aff2f22af43389289f492f44933b7ce0fd4b2546dcd5aec60"
train_universe = 655461
heldout_ids_file   = "${paths.repo}/experiments/prs/EPR-051_masked-restore-production/stage0/snapshot_newdata_v3.heldout_ids.json"
heldout_ids_sha256 = "bc8b3ea776985ccf2314782c0d7ee327ef6563ce8cdb8d99a04b9569fddaa22d"
expect_heldout_samples = 4560

[bk]
# BK-FULL：backend = ADAGN+FF+AFFHEAD（stage_flow_bk4），其余与 arm_clut_full 逐键相同
arm = "adagn_ff_affhead"
layer_checkpoint = true
gn_groups = 8   # RDBM Block groups=8
n_freq = 8   # MetaDC color_pe 8 freq
