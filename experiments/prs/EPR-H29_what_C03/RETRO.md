# EPR-H29 C03(OracleWhere-FG48,ceiling 臂)

状态: REJECTED(未运行即移除;**非对 ceiling 假设的否决**)
目标指标: oracle Where latent(cband12)上界参照;判据门同 C 波(预注册)。
输入: ——(零)
输出: 零产物(`/home/bc/data/runs/what/C03/` 与 `/home/bc/data/runs/what/evaluate/C03/` 均为空目录)
指标变化: 无
结论一句话: 从未启动——先被队列 `q reorder first` 的交换语义缺陷甩到自己评测任务之后(gpu0 空转 5h21m),后随 2026-08-11 用户裁定「What 全线停摆」从队列移除;若启动会与 C04 同因崩溃(oracle readout 覆盖缺口)。
冲突/修正记录: REJECTED 指「未运行即移除」,不是对 ceiling 假设的否决;待 Where 定案后再议。
