"""PR-AMORT: the two amortised-fitting arms (P1, P3') and their shared plumbing.

Lazy re-exports only; importing the package must not pull in torch-heavy or
NFS-touching modules as a side effect.
"""
