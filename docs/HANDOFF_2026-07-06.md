# 交接文档：5 万高质量样本冲刺（2026-07-06 16:00 交接）

## 一、任务与当前状态

**验收目标**：global + degraded 两链合计 **50,000 条高质量 img 样本**，每功能一个 commit。

| 产物 | 数量 | 位置 |
|---|---:|---|
| degrade 链样本（已定格） | **25,626** | `/home/bc/data/datasets/vera_directionA_1M/degrade_v3/samples.jsonl`（images/ 同目录） |
| global SFT | **~6,200**（在产） | `.../construct_global_v3/sft.jsonl` |
| global DPO | ~430 | 同目录 dpo.jsonl |
| 回收 SFT（retier） | 0（进程反复被环境问题打断） | 同目录 sft_recovered.jsonl |
| **合计** | **~31,800 / 50,000** | 缺口 ~18,000，由 global build + retier 回收补 |

## 二、⚠️ 当前最高优先级问题（接手先修这个）

**construct build / retier 进程启动后卡死在模型加载，双卡空闲。**
- 症状：进程活着但 CPU ~0，`global_run.log` 停在 ArtiMuse `Loading checkpoint shards 4/4` 后无输出，无 `[N/16000]` 进度行。
- 已试：`HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`（怀疑 Charm 的 `facebook/dinov2-large` 卡联网 etag）——**用户反馈仍空闲，未根治**。
- 下一步诊断建议：
  1. 单独复现加载：`HF_HUB_OFFLINE=1 PYTHONPATH=... /home/bc/.venvs/iaa437/bin/python -c "from construct import objscore; r=objscore._runner(); r.load(); print('ok')"`，看卡在 ArtiMuse 还是 Charm；
  2. `sudo py-spy dump --pid <pid>`（普通用户被拒，试 sudo）或 `cat /proc/<pid>/stack`、`ls -l /proc/<pid>/fd` 看是否卡网络 fd；
  3. 排查 annotate 预热/PG 连接/`_caps()`（8027 行 caption 加载）等 warmup 链的其他环节；
  4. 若 Charm 是元凶且离线 env 无效，考虑在 iaa.py 的 Charm Backbone 加载处显式传本地缓存路径（HF snapshot 目录在 `~/.cache/huggingface/hub/models--facebook--dinov2-large`）。
  5. **注意**：昨夜 IAA 回填（同样加载 Charm）一切正常——对比当时环境（进程由 iaa.py CLI 直启）与现在（construct.agent 里 warmup）之间的差异。

## 三、生产进程与启动命令

```bash
# 1) global build（主产线，resume 安全，产率健康时 ~1.7 SFT/组、~700 组/h）
/tmp/claude-1001/-home-bc-VeraRetouch/7de0cac7-5539-4693-8708-2317262ce703/scratchpad/global_run.sh
# 内容：cd /home/bc/VeraRetouch; source .env; PYTHONPATH=repo:repo/dataset_build/src;
# RENDER_BACKEND_POLICY=throughput; SOURCE_QA_IAA_DEVICE=cuda:0; HF_HUB_OFFLINE=1;
# /home/bc/.venvs/iaa437/bin/python -m construct.agent run --n 16000 --render-n 8 \
#   --src-workers 24 --out .../construct_global_v3

# 2) retier（污染窗口回收，预期 +2500 SFT）
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTHONPATH=/home/bc/VeraRetouch:/home/bc/VeraRetouch/dataset_build/src \
SOURCE_QA_IAA_DEVICE=cuda:1 CONSTRUCT_IAA_DEVICE=cuda:1 \
/home/bc/.venvs/iaa437/bin/python -m construct.retier \
  --out /home/bc/data/datasets/vera_directionA_1M/construct_global_v3 --from-idx 250 --to-idx 2150

# 3) 存量重标注（v2 文本升级；500 条试跑已 100% 通过 @109/min；全量 degrade ~4h）
PYTHONPATH=同上 python -m construct.reannotate \
  --in /home/bc/data/datasets/vera_directionA_1M/degrade_v3/samples.jsonl --kind degrade --workers 16
# global 侧：--in .../construct_global_v3/sft.jsonl --kind global（v1 期样本；v2 期产出无需重标）

# 4) vLLM 35B（annotate/verify 依赖）：docker 容器 reason_g1（GPU1, :8002, util 0.70），broker :8003
# 挂了就 docker start reason_g1，就绪 ~10-15 分钟；vLLM 重启后必须重启 build/retier（annotate 熔断复位）
```

## 四、硬约束（务必遵守）

- **卡 0 我方总占用 ≤47GB**（给其他同学留 50GB）；**卡 1 可全占**。QA-IAA（~25GB）目前约定：build 用 cuda:0，retier 用 cuda:1。
- **卡 0 绝不跑我方大服务**；zhengh 等他人任务在两卡上来去，属正常。
- 每完成一个功能 **git commit**（分支 `archive/qa-analysis-datagen-v2`，尾注 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`），保持仓库干净。
- 农场（3 台 LR client）是生产资源：并发 gate=6，别提交海量无谓渲染；操作 client 遵守 catalog 竞态保护流程。

## 五、已完成（26 commits，全部在分支上）

渲染：gpu_render 迁移（parity 逐位）/ teacher render 废弃 → 农场+本地双路（throughput 策略，本地可渲 56%）。
数据：IAA 回填 86,112 张零失败（优化 25×）；退化链 v1（论文 Tab8 σ 逐值对齐 + 质量门 + 全 VLM 标注）；场景/风格配额混入（mixing）；annotate v2（img caption + 三方面 CoT + metric 注入 + 8 persona 去模板化，验收：前缀 top1 3.3-6.7%、CoT ≥96.7%、metric ≥96.7%）；reannotate 存量升级工具（500 试跑 100% 过）。
农场：bc-home/sysu catalog 归零 + 修复版启动脚本部署 + reset 周期实测 3+ 轮成功；跨机 ΔE 一致。

## 六、已知坑（都踩过，别再踩）

1. **批打分禁用**：`objscore.score_many` 在 construct 生产进程里把分数压低 12-19 分（qa.py 有注释）——别重新启用；单张 0.13s 足够。
2. **ArtiMuse 加载 shim 竞争**：必须在 lanes 并发前单线程 warmup（agent.py 已有）；跳过 warmup 会让打分退化成 46-50 窄带常数。
3. **iaa437 venv**：construct/QA-IAA 必须用 `/home/bc/.venvs/iaa437/bin/python`（base 的 transformers 4.36 无 Qwen2，现已 fail-fast）。
4. **pkill 自杀**：`pkill -f "关键词"` 会匹配自己 shell 的 cmdline —— 用 `for pid in $(pgrep -f '模式加[]转义'); do kill $pid; done`。
5. **zsh noclobber**：重定向已存在文件用 `>|`。
6. **TAU_SFT=0.55 不要乱动**：产率崩时先查打分链（见坑 1/2），别急着调阈值/加 iaa 上界（`CONSTRUCT_SOURCE_IAA_MAX` 之前的补丁是误诊，现已不设）。
7. **mixing 源池不要求 saturation_mean**（老 stage0 标记，会饿死池子，已移除）。
8. vLLM 重启后 build/retier 必须跟着重启（annotate 熔断按进程锁存）。

## 七、验收流程（达 50k 后）

1. 统计：`degrade samples + sft + sft_recovered ≥ 50000`（重标注产物 .v2.jsonl 为文本升级版，同一样本不重复计数）。
2. 质检汇总：两链各抽 50 条——泄露断言（GT 参数/风格名按任务规则）、三方面 CoT 覆盖、metric 引用、前缀 top1 多样性、图像对可视合理；degrade 全过 ΔE/clip 门、global 全过 q≥0.55。
3. 事故披露：污染窗口（07-06 07:32 CUDA 故障、08:47-11:40 打分退化）的处理与 DPO ~130 对存疑标记。
4. 写 `docs/ACCEPTANCE_50K_<date>.md` + commit；更新任务清单 #19。

## 八、遗留（非阻塞，用户已知）

- Track-A（E+R 专家反演）零实现；region-local 退化 v2 未做。
- scene="any" 桶占比偏高，可用 vLLM 空闲期细分类后补波次。
- 存量 DPO ~130 对（污染窗口）建议过滤或重建。
- local 链 annotate 均价 ~2.2 次/样本（防泄露重试），预算紧可调 max_leak_retries=1。
- 监控：桌面有 vLLM 容器/global 进程守护 Monitor 与 IAA 监控残留（本 session 的，接手后自建）。
