# 复制为 docker/ms-swift/.env 后再 `docker compose up -d`（.env 已 gitignore）。
# 本机 2026-09-06 核实：NVIDIA driver 570.195.03（CUDA 12.8），H100 ×2；cuda13.x 镜像需 driver ≥ 580，不可用。
# 以下 tag 均抄自 ms-swift 官方安装文档「Docker」节（打开核实过），按本机驱动选 cuda12.8.1 版：
MS_SWIFT_IMAGE=modelscope-registry.cn-hangzhou.cr.aliyuncs.com/modelscope-repo/modelscope:ubuntu22.04-cuda12.8.1-py311-torch2.10.0-vllm0.17.1-modelscope1.34.0-swift4.0.3
# 备选（同文档；需先升级驱动到 ≥ 580 才能用 cuda13.0.3 的最新 swift4.4.3）：
# MS_SWIFT_IMAGE=modelscope-registry.cn-hangzhou.cr.aliyuncs.com/modelscope-repo/modelscope:ubuntu22.04-cuda13.0.3-py312-torch2.11.0-vllm0.25.1-modelscope1.39.0-swift4.4.3
# 旧版（cuda12.6.3，driver ≥ 560 即可）：
# MS_SWIFT_IMAGE=modelscope-registry.cn-hangzhou.cr.aliyuncs.com/modelscope-repo/modelscope:ubuntu22.04-cuda12.6.3-py311-torch2.7.1-vllm0.10.1.1-modelscope1.29.2-swift3.8.3
CONTAINER_NAME=vr-ms-swift
# 容器内 [paths] 覆盖里的 LUT bank（compose 已挂到 /lut_bank）
VR_PATH_LUT_BANK=/lut_bank
